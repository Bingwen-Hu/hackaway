/**
unsigned types are the most convenient types
without overflow and can be optimized best

use size_t for sizes, cardinalities or ordinal numbers.
*/

#include <stdio.h>

void main(){

    signed short a = -32768;
    printf("value of a is: %d\n", a);
    puts("first_line\n"
         "another_line\n");


}
