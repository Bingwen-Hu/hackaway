/* mory version */



#include <stdio.h>


int main() {
    int line;
    printf("input number of lines: (1-12)");
    scanf("%d", &line);
    
    for (int i = 0; i < line; i++){
        // if I still living, I will write it!
    }
}