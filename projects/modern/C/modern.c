#pragma once

// When a standard is not enough to clear everything
// convention comes in
// Every function ends with `M` means memory is allocated
// Every function ends with `F` means memory is freed.
// It seems the 'Man' make things mess and 'Female' make
// it good.

